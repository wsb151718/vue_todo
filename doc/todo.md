# Vue 開発時の設定項目

## ✅ 必須設定

### 1. ESLint 設定（必須）

- [x] ESLint のインストールと設定
  - `eslint.config.js`の設定確認
  - Vue 用の ESLint プラグイン（`eslint-plugin-vue`）の設定確認
  - Prettier との統合設定（`@vue/eslint-config-prettier`）の確認
  - 実行コマンド: `npm run lint`

### 2. Prettier 設定

- [x] Prettier のインストールと設定
  - `.prettierrc.json`の設定確認
  - フォーマットコマンド: `npm run format`

### 3. パスエイリアス設定

- [x] `@`エイリアスの設定
  - `vite.config.js`でのエイリアス設定確認
  - `jsconfig.json`でのパス設定確認

### 4. Git 設定

- [x] `.gitignore`の設定確認
  - `node_modules`、`dist`、`.eslintcache`などの除外設定

## 📋 推奨設定

### 5. エディタ設定（VS Code 推奨）

- [x] `.vscode/settings.json`の作成
  - 保存時の自動フォーマット設定
  - ESLint の自動修正設定
  - ファイル保存時の自動フォーマット有効化

### 6. 環境変数設定

- [x] `.env`ファイルの作成（必要に応じて）
  - `.env.local`（ローカル開発用、Git 管理外）
  - `.env.development`（開発環境用）
  - `.env.production`（本番環境用）
  - `.env.example`（テンプレート、Git 管理）

### 7. 開発ツール設定

- [x] Vue DevTools の設定確認
  - `vite-plugin-vue-devtools`の設定確認

### 8. その他の推奨設定

- [x] `.editorconfig`の作成（エディタ間の一貫性確保）

## 🔧 設定確認コマンド

```bash
# 依存関係のインストール確認
npm install

# ESLintの実行
npm run lint

# Prettierのフォーマット実行
npm run format

# 開発サーバーの起動
npm run dev

# ビルドの確認
npm run build
```

## 📝 注意事項

- ESLint の設定は絶対に必須です
- Prettier と ESLint の競合を避けるため、`@vue/eslint-config-prettier`を使用
- パスエイリアス（`@`）を使用することで、相対パスの深い階層を避けられます
- 環境変数は機密情報を含む可能性があるため、`.gitignore`に適切に設定
